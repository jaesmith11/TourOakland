<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--style of toolbar-->
    <style name="ToolbarFont" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:fontFamily">@font/bungee_shade</item>
    </style>

    <!--style of tabs, note parent is the TabWidget-->
    <style name="TabTextStyle" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:fontFamily">@font/lato</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <!--style of headings-->
    <style name="headingStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:paddingLeft">12dp</item>
        <item name="android:fontFamily">@font/bungee_shade</item>
    </style>

    <!--style of subtext on activity_detail.xml-->
    <style name="detailStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAppearance">?android:textAppearanceSmall</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:paddingLeft">12dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:fontFamily">@font/lato</item>
    </style>

    <!--style of white cards-->
    <style name="cardLayoutStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">8dp</item>
        <item name="android:layout_marginRight">8dp</item>
        <item name="android:background">@drawable/customborder</item>
        <item name="android:orientation">vertical</item>
    </style>

</resources>
